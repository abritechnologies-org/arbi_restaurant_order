spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: arbi-tech-config-server
  application:
    name: arbi-tech-restaurant-order
  profiles:
    active: prod
management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
  endpoint:
    env:
      post:
        enabled: true

